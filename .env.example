# DEEPSEEK_API_KEY is only needed if you bypass the opencode-go gateway and use
# the native DeepSeek provider directly. Export it in your shell, never commit it.
DEEPSEEK_API_KEY=

# Optional: override the opencode binary used by scripts/run.sh.
# OPENCODE_BIN=opencode
